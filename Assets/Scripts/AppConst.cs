public static class AppConst
{
    // 是否打印LOG
    public static readonly bool kLogEnabled = true;

    // 是否第三方接收LOG信息
    public static readonly bool kLogMessageReceived = false;

    // 加载资源的agent个数
    public static readonly int kResourceAgentCount = 3;

    // 消息密钥
    public static ulong kSecret = 0;
}
